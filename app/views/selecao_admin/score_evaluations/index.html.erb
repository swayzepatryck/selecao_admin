<%- model_class = SelecaoAdmin::ScoreEvaluation -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize %></h1>
</div>

<div class="form-actions">
	<%= link_to 'Criar', "#", :"data-controls-modal" => "newScoreEvaluationModal", :"data-backdrop" => true, :"data-keyboard" => true, :class => "btn btn-primary", :onmouseover => "sandwich='pastrami';"  %>
	<%= link_to t('.back', :default => t("helpers.links.back")),
	            SelecaoAdmin::path, :class => 'btn'  %>
</div>

<table class="table table-striped" id="scoreEvaluationAjaxTable">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:title) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @score_evaluations.each do |score_evaluation| %>
      <tr id="tr_<%=score_evaluation.id%>">
        <td><%= link_to score_evaluation.id, score_evaluation_path(score_evaluation) %></td>
        <td><%= link_to score_evaluation.title, score_evaluation_path(score_evaluation)  %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_score_evaluation_path(score_evaluation), :class => 'btn btn-mini', :remote => true %>

					<%= link_to_if(any_restrict_dependecy?(score_evaluation), t('.destroy', :default => t("helpers.links.destroy")), "#", :class => "btn btn-mini btn-danger", :rel => "popover", :"data-content" => t("validation.restrict_validation", :dependecie_models => any_restrict_dependecy?(score_evaluation)), :"data-original-title" => t("selecao_admin.general.atention")) {%>					
	          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
	                      score_evaluation_path(score_evaluation),
	                      :method => :delete,
	                      :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')),
	                      :class => 'btn btn-mini btn-danger',
	 											:remote => true %>
					<%}%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<div id="newScoreEvaluationModal" class="modal hide fade" style="padding: 20px; max-height: 500px;">
  <%= render :partial => 'form' %>	
</div>




 	