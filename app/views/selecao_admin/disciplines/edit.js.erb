$("#main").before("<%= escape_javascript(render 'edit', :formats => [:html]) %>");
$("#editDisciplineModal").modal({
	show: true,
	backdrop: true,
	keyboard: true	
}).on('hidden', function () {
		$('#editDisciplineModal').remove();
});

