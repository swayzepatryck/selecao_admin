<%- if @student_quota.errors.any? %>
  console.log('Error');
  $('#newStudentQuotaModal').html('<%= escape_javascript(render('form')) %>');
<%- else %>
	console.log('Created');
  $('#newStudentQuotaModal').modal('hide');
	var addId = $('#studentQuotaAjaxTable').dataTable().fnAddData( [
		"<%=escape_javascript(link_to @student_quota.id, student_quota_path(@student_quota))%>",
		"<%=escape_javascript(link_to @student_quota.title, student_quota_path(@student_quota))%>",
		"<%=escape_javascript(@student_quota.abbreviation)%>",
		"<%=escape_javascript(link_to t('.edit', :default => t('helpers.links.edit')), edit_student_quota_path(@student_quota), :class => 'btn btn-mini')%> <%=escape_javascript(link_to t('.destroy', :default => t('helpers.links.destroy')), student_quota_path(@student_quota), :method => :delete, :class => 'btn btn-mini btn-danger', :confirm => t('.confirm', :default => t('helpers.links.confirm', :default => 'Are you sure?')), :remote => true)%>"		
	]);	
 	var theNode = $('#studentQuotaAjaxTable').dataTable().fnSettings().aoData[addId[0]].nTr;
	$(theNode).attr("id", "tr_<%=@student_quota.id%>");		
<%- end %>
